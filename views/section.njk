<!DOCTYPE html>
<html>
    <head>
        <title>{{res.locals.title}}</title>
    </head>
    <body>
        <ul>
        {% for section in sections %}
            <h2>{{section.name}}</h2>
            <p>{{section.id}}</p>
        {% endfor %}
        </ul>
    </body>
</html>